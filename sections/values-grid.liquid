{% comment %} 
                                                                  Values Grid Section - Estilo Peloton
                                                                  Seção com missão centralizada + grid de 3 valores/pilares 
{% endcomment %}

<section class="values-grid" data-section-id="{{ section.id }}">
  <div class="values-grid__container">
    <!-- Mission Statement -->
    {% if section.settings.mission_heading != blank %}
      <div class="values-grid__mission">
        {% if section.settings.mission_label != blank %}
          <p class="values-grid__mission-label condensed-bold">{{ section.settings.mission_label }}</p>
        {% endif %}
        <h2 class="values-grid__mission-heading black">{{ section.settings.mission_heading }}</h2>
      </div>
    {% endif %}

    <!-- Values Title -->
    {% if section.settings.values_heading != blank %}
      <div class="values-grid__values-header">
        <h3 class="values-grid__values-heading bold">{{ section.settings.values_heading }}</h3>
      </div>
    {% endif %}

    <!-- Values Grid -->
    {% if section.blocks.size > 0 %}
      <div class="values-grid__grid">
        {% for block in section.blocks %}
          <div class="values-grid__item" {{ block.shopify_attributes }}>
            {% if block.settings.number != blank %}
              <div class="values-grid__number">
                <span class="black">{{ block.settings.number }}</span>
              </div>
            {% endif %}

            {% if block.settings.title != blank %}
              <h4 class="values-grid__item-title bold">{{ block.settings.title }}</h4>
            {% endif %}

            {% if block.settings.text != blank %}
              <p class="values-grid__item-text regular">{{ block.settings.text }}</p>
            {% endif %}
          </div>
        {% endfor %}
      </div>
    {% endif %}
  </div>
</section>

{% style %}
  .values-grid {
    background: #ffffff;
    margin: 0;
  }

  .values-grid__container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
  }

  /* Mission Section */
  .values-grid__mission {
    text-align: center;
    margin-bottom: 40px;
  }

  .values-grid__mission-label {
    font-size: 16px;
    color: #666666;
    margin-bottom: 20px;
    text-transform: uppercase;
    letter-spacing: 2px;
    font-weight: 400;
  }

  .values-grid__mission-heading {
    font-size: 48px;
    font-weight: 700;
    color: #000000;
    line-height: 1.3;
    letter-spacing: -0.5px;
    max-width: 900px;
    margin: 0 auto;
  }

  /* Values Header */
  .values-grid__values-header {
    text-align: center;
    margin-bottom: 60px;
  }

  .values-grid__values-heading {
    font-size: 36px;
    color: #000000;
    margin: 0;
  }

  /* Values Grid */
  .values-grid__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 60px;
    margin-top: 60px;
  }

  .values-grid__item {
    text-align: center;
    padding: 0 20px;
  }

  .values-grid__number {
    width: 60px;
    height: 60px;
    border: 2px solid #cccccc;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 30px;
    position: relative;
  }

  .values-grid__number span {
    font-size: 24px;
    font-weight: 700;
    color: #000000;
  }

  .values-grid__item-title {
    font-size: 24px;
    font-weight: 600;
    color: #000000;
    margin-bottom: 20px;
    line-height: 1.3;
  }

  .values-grid__item-text {
    font-size: 16px;
    color: #666666;
    line-height: 1.6;
    margin: 0;
  }

  /* Responsive */
  @media screen and (max-width: 1024px) {
    .values-grid__grid {
      gap: 50px;
    }
  }

  @media screen and (max-width: 768px) {
    .values-grid {
      padding: 0 0 70px;
      margin: 0;
    }

    .values-grid__container {
      padding: 0 15px;
    }

    .values-grid__mission {
      margin-bottom: 50px;
    }

    .values-grid__mission-label {
      font-size: 14px;
      margin-bottom: 15px;
      letter-spacing: 1.5px;
    }

    .values-grid__mission-heading {
      font-size: 32px;
    }

    .values-grid__values-header {
      margin-bottom: 40px;
    }

    .values-grid__values-heading {
      font-size: 28px;
    }

    .values-grid__grid {
      grid-template-columns: 1fr;
      gap: 40px;
      margin-top: 40px;
    }

    .values-grid__item {
      padding: 0 10px;
    }

    .values-grid__number {
      width: 50px;
      height: 50px;
      margin-bottom: 25px;
    }

    .values-grid__number span {
      font-size: 20px;
    }

    .values-grid__item-title {
      font-size: 22px;
      margin-bottom: 15px;
    }

    .values-grid__item-text {
      font-size: 15px;
    }
  }

  @media screen and (max-width: 480px) {
    .values-grid__mission-heading {
      font-size: 26px;
    }

    .values-grid__values-heading {
      font-size: 24px;
    }

    .values-grid__item-title {
      font-size: 20px;
    }

    .values-grid__item-text {
      font-size: 14px;
    }
  }
{% endstyle %}

{% schema %}
  {
    "name": "Values Grid",
    "tag": "section",
    "class": "section",
    "settings": [
      {
        "type": "header",
        "content": "Mission Section"
      },
      {
        "type": "text",
        "id": "mission_label",
        "label": "Mission Label",
        "default": "Our mission"
      },
      {
        "type": "text",
        "id": "mission_heading",
        "label": "Mission Heading",
        "default": "Bring integrated fitness and wellness experiences to Members anytime, anywhere"
      },
      {
        "type": "header",
        "content": "Values Section"
      }, {
        "type": "text",
        "id": "values_heading",
        "label": "Values Heading",
        "default": "Our values"
      }
    ],
    "blocks": [
      {
        "type": "value",
        "name": "Value",
        "settings": [
          {
            "type": "text",
            "id": "number",
            "label": "Number",
            "default": "1"
          }, {
            "type": "text",
            "id": "title",
            "label": "Title",
            "default": "Believe"
          }, {
            "type": "textarea",
            "id": "text",
            "label": "Text",
            "default": "We believe in the power of Ativafit to change people's lives."
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "Values Grid",
        "settings": {
          "mission_label": "Our mission",
          "mission_heading": "Bring integrated fitness and wellness experiences to Members anytime, anywhere",
          "values_heading": "Our values"
        },
        "blocks": [
          {
            "type": "value",
            "settings": {
              "number": "1",
              "title": "Believe",
              "text": "We believe in the power of Ativafit to change people's lives."
            }
          }, {
            "type": "value",
            "settings": {
              "number": "2",
              "title": "Bring your best",
              "text": "We always bring our best. We put Members first, delivering products and experiences for which we can be proud."
            }
          }, {
            "type": "value",
            "settings": {
              "number": "3",
              "title": "Lift people up",
              "text": "We lift each other up, knowing that together we go far."
            }
          }
        ]
      }
    ]
  }
{% endschema %}