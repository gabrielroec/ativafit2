{%- liquid
  assign bss_id = 'bundle-step-support-' | append: section.id
  assign radio_name = 'bundle-support-storage-' | append: section.id
  assign bss_default_slot = 0
  assign bss_first_slot = 0

  for block in section.blocks
    if block.type == 'support_option'
      assign bss_prod = block.settings.product
      if bss_prod != blank
        assign bss_var = bss_prod.selected_or_first_available_variant
        if bss_var != blank
          if bss_first_slot == 0
            assign bss_first_slot = forloop.index
          endif
          if block.settings.default_selected and bss_default_slot == 0
            assign bss_default_slot = forloop.index
          endif
        endif
      endif
    endif
  endfor

  if bss_default_slot == 0
    assign bss_default_slot = bss_first_slot
  endif
-%}

<section
  id="{{ bss_id }}"
  class="bundle-step-support-storage"
  data-section-type="bundle-step-support-storage"
  {{ section.shopify_attributes }}>
  <div class="bundle-step-support-storage__inner">
    <h2 class="bundle-step-support-storage__heading">
      <span class="bundle-step-support-storage__heading-num">{{ section.settings.step_number | escape }}</span>
      <span class="bundle-step-support-storage__heading-text">{{ section.settings.step_title | escape }}</span>
    </h2>

    <fieldset class="bundle-step-support-storage__fieldset">
      <legend class="visually-hidden">{{ section.settings.fieldset_legend | escape }}</legend>
      <div class="bundle-step-support-storage__grid" role="radiogroup" aria-label="{{ section.settings.fieldset_legend | escape }}">
        {%- for block in section.blocks -%}
          {%- if block.type == 'support_option' -%}
            <div class="bundle-step-support-storage__cell" {{ block.shopify_attributes }}>
              {%- assign opt_product = block.settings.product -%}
              {%- if opt_product != blank -%}
                {%- assign opt_variant = opt_product.selected_or_first_available_variant -%}
                {%- assign input_id = 'bundle-support-' | append: section.id | append: '-' | append: forloop.index -%}
                {%- assign is_checked = false -%}
                {%- if forloop.index == bss_default_slot -%}
                  {%- assign is_checked = true -%}
                {%- endif -%}
                {%- render 'bundle-option-card',
                  product: opt_product,
                  variant: opt_variant,
                  input_id: input_id,
                  input_name: radio_name,
                  checked: is_checked,
                  card_title: block.settings.card_title,
                  card_description: block.settings.card_description,
                  badge_label: block.settings.badge_label,
                  line_category: block.settings.sidebar_category,
                  line_icon: block.settings.sidebar_icon
                -%}
              {%- else -%}
                <div class="bundle-step-support-storage__placeholder">
                  <span class="bundle-step-support-storage__placeholder-inner">{{ section.settings.empty_state_text | escape }}</span>
                </div>
              {%- endif -%}
            </div>
          {%- endif -%}
        {%- endfor -%}
      </div>
    </fieldset>
  </div>
</section>

<style>
  #{{ bss_id }}.bundle-step-support-storage {
    padding-top: {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom }}px;
    background-color: {{ section.settings.background_color }};
  }

  #{{ bss_id }} .bundle-step-support-storage__inner {
    max-width: 80rem;
    margin: 0 auto;
    padding: 0 24px;
  }

  #{{ bss_id }} .bundle-step-support-storage__heading {
    font-family: var(--heading-font-family) !important;
    margin: 0 0 {{ section.settings.heading_margin_bottom }}px !important;
    line-height: 1.05 !important;
    text-transform: uppercase !important;
    letter-spacing: -0.03em !important;
  }

  #{{ bss_id }} .bundle-step-support-storage__heading-num {
    color: {{ section.settings.step_number_color }} !important;
    font-weight: 800 !important;
    font-size: {{ section.settings.heading_num_size_mobile }}px !important;
    margin-right: 0.35em !important;
  }

  #{{ bss_id }} .bundle-step-support-storage__heading-text {
    color: {{ section.settings.step_title_color }} !important;
    font-weight: 900 !important;
    font-size: {{ section.settings.heading_title_size_mobile }}px !important;
  }

  @media screen and (min-width: 750px) {
    #{{ bss_id }} .bundle-step-support-storage__heading-num {
      font-size: {{ section.settings.heading_num_size_desktop }}px !important;
    }
    #{{ bss_id }} .bundle-step-support-storage__heading-text {
      font-size: {{ section.settings.heading_title_size_desktop }}px !important;
    }
  }

  #{{ bss_id }} .bundle-step-support-storage__fieldset {
    border: none;
    margin: 0;
    padding: 0;
    min-width: 0;
  }

  #{{ bss_id }} .bundle-step-support-storage__grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: {{ section.settings.grid_gap }}px;
    align-items: stretch;
  }

  @media screen and (min-width: 750px) {
    #{{ bss_id }} .bundle-step-support-storage__grid {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }
  }

  #{{ bss_id }} .bundle-step-support-storage__cell {
    min-width: 0;
    display: flex;
    flex-direction: column;
    align-self: stretch;
    min-height: 0;
  }

  #{{ bss_id }} .bundle-option-card {
    display: flex;
    flex-direction: column;
    flex: 1;
    min-height: 0;
    height: 100%;
    cursor: pointer;
    margin: 0;
  }

  #{{ bss_id }} .bundle-option-card__surface {
    display: flex;
    flex-direction: column;
    flex: 1;
    min-height: 0;
    height: 100%;
    background: #ffffff;
    border-radius: 10px;
    border: 1px solid #e5e5e5;
    overflow: hidden;
    transition: border-color 0.2s ease, box-shadow 0.2s ease;
  }

  #{{ bss_id }} .bundle-option-card__input:checked + .bundle-option-card__surface {
    border-width: 3px;
    border-color: {{ section.settings.selection_border_color }};
    box-shadow: 0 0 0 1px {{ section.settings.selection_border_color }};
  }

  #{{ bss_id }} .bundle-option-card__input:focus-visible + .bundle-option-card__surface {
    outline: 2px solid {{ section.settings.selection_border_color }};
    outline-offset: 3px;
  }

  #{{ bss_id }} .bundle-option-card__media {
    position: relative;
    background: #f2f2f2;
    aspect-ratio: 1 / 1;
    overflow: hidden;
  }

  #{{ bss_id }} .bundle-option-card__img,
  #{{ bss_id }} .bundle-option-card__placeholder {
    width: 100%;
    height: 100%;
    object-fit: contain;
    display: block;
  }

  #{{ bss_id }} .bundle-option-card__check {
    position: absolute;
    top: 10px;
    right: 10px;
    width: 30px;
    height: 30px;
    border-radius: 50%;
    background: {{ section.settings.selection_border_color }};
    color: #ffffff;
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    transform: scale(0.85);
    transition: opacity 0.2s ease, transform 0.2s ease;
    pointer-events: none;
    z-index: 2;
  }

  #{{ bss_id }} .bundle-option-card__input:checked + .bundle-option-card__surface .bundle-option-card__check {
    opacity: 1;
    transform: scale(1);
  }

  #{{ bss_id }} .bundle-option-card__body {
    padding: 16px;
    display: flex;
    flex-direction: column;
    gap: 8px;
    align-items: center;
    text-align: center;
    flex: 1;
  }

  #{{ bss_id }} .bundle-option-card__badge {
    font-family: var(--heading-font-family) !important;
    font-size: 11px !important;
    font-weight: 700 !important;
    letter-spacing: 0.08em !important;
    text-transform: uppercase !important;
    color: {{ section.settings.selection_border_color }} !important;
    background: rgba(235, 112, 31, 0.14) !important;
    padding: 5px 10px !important;
    border-radius: 6px !important;
    line-height: 1.2 !important;
  }

  #{{ bss_id }} .bundle-option-card__title {
    font-family: var(--heading-font-family) !important;
    font-size: 16px !important;
    line-height: 1.3 !important;
    font-weight: 700 !important;
    color: #000000 !important;
    margin: 0 !important;
  }

  #{{ bss_id }} .bundle-option-card__desc {
    font-family: var(--text-font-family) !important;
    font-size: 14px !important;
    line-height: 1.45 !important;
    font-weight: var(--text-font-weight) !important;
    color: {{ section.settings.description_color }} !important;
    margin: 0 !important;
  }

  #{{ bss_id }} .bundle-option-card__price-row {
    display: flex;
    align-items: baseline;
    justify-content: center;
    gap: 8px;
    flex-wrap: wrap;
    margin-top: auto;
    padding-top: 4px;
    width: 100%;
  }

  /* Preço preto por defeito; laranja só no card selecionado (mockup step 2) */
  #{{ bss_id }} .bundle-option-card__price {
    font-family: var(--heading-font-family) !important;
    font-size: 18px !important;
    font-weight: 700 !important;
    color: #000000 !important;
    line-height: 1 !important;
  }

  #{{ bss_id }} .bundle-option-card__input:checked + .bundle-option-card__surface .bundle-option-card__price {
    color: {{ section.settings.selection_border_color }} !important;
  }

  #{{ bss_id }} .bundle-option-card__compare {
    font-size: 14px !important;
    color: #555555 !important;
    text-decoration: line-through !important;
  }

  #{{ bss_id }} .bundle-step-support-storage__placeholder {
    border: 2px dashed #e0e0e0;
    border-radius: 10px;
    flex: 1;
    min-height: 280px;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 24px;
    text-align: center;
    background: #fafafa;
  }

  #{{ bss_id }} .bundle-step-support-storage__placeholder-inner {
    font-family: var(--text-font-family) !important;
    font-size: 14px !important;
    color: #888888 !important;
  }
</style>

{% schema %}
{
  "name": "Step 2: support storage",
  "tag": "section",
  "class": "section-bundle-step-support-storage",
  "max_blocks": 3,
  "settings": [
    {
      "type": "text",
      "id": "step_number",
      "label": "Número do passo",
      "default": "02"
    },
    {
      "type": "text",
      "id": "step_title",
      "label": "Título do passo",
      "default": "SUPPORT & STORAGE"
    },
    {
      "type": "text",
      "id": "fieldset_legend",
      "label": "Legenda do grupo (acessibilidade)",
      "default": "Choose support and storage"
    },
    {
      "type": "text",
      "id": "empty_state_text",
      "label": "Texto quando o produto não está definido",
      "default": "Select a product in this block"
    },
    {
      "type": "header",
      "content": "Cores"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Fundo da secção",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "step_number_color",
      "label": "Cor do número (ex.: 02)",
      "default": "#e8b896"
    },
    {
      "type": "color",
      "id": "step_title_color",
      "label": "Cor do título",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "description_color",
      "label": "Cor da descrição nos cards",
      "default": "#574237"
    },
    {
      "type": "color",
      "id": "selection_border_color",
      "label": "Cor de seleção (borda + check + preço ativo)",
      "default": "#eb701f"
    },
    {
      "type": "header",
      "content": "Tipografia"
    },
    {
      "type": "range",
      "id": "heading_num_size_mobile",
      "min": 24,
      "max": 40,
      "step": 1,
      "unit": "px",
      "label": "Tamanho do número — mobile",
      "default": 32
    },
    {
      "type": "range",
      "id": "heading_num_size_desktop",
      "min": 28,
      "max": 48,
      "step": 1,
      "unit": "px",
      "label": "Tamanho do número — desktop",
      "default": 36
    },
    {
      "type": "range",
      "id": "heading_title_size_mobile",
      "min": 20,
      "max": 36,
      "step": 1,
      "unit": "px",
      "label": "Tamanho do título — mobile",
      "default": 26
    },
    {
      "type": "range",
      "id": "heading_title_size_desktop",
      "min": 24,
      "max": 44,
      "step": 1,
      "unit": "px",
      "label": "Tamanho do título — desktop",
      "default": 32
    },
    {
      "type": "range",
      "id": "heading_margin_bottom",
      "min": 16,
      "max": 48,
      "step": 2,
      "unit": "px",
      "label": "Espaço abaixo do título",
      "default": 28
    },
    {
      "type": "range",
      "id": "grid_gap",
      "min": 12,
      "max": 32,
      "step": 2,
      "unit": "px",
      "label": "Espaço entre cards",
      "default": 20
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 24,
      "max": 96,
      "step": 4,
      "unit": "px",
      "label": "Padding superior",
      "default": 48
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 24,
      "max": 96,
      "step": 4,
      "unit": "px",
      "label": "Padding inferior",
      "default": 48
    }
  ],
  "blocks": [
    {
      "type": "support_option",
      "name": "Opção",
      "limit": 3,
      "settings": [
        {
          "type": "product",
          "id": "product",
          "label": "Produto"
        },
        {
          "type": "text",
          "id": "card_title",
          "label": "Título no card (opcional)",
          "info": "Se vazio, usa o título do produto."
        },
        {
          "type": "textarea",
          "id": "card_description",
          "label": "Descrição curta"
        },
        {
          "type": "text",
          "id": "badge_label",
          "label": "Badge (opcional)",
          "info": "Deixe vazio para ocultar."
        },
        {
          "type": "checkbox",
          "id": "default_selected",
          "label": "Pré-selecionado",
          "default": false,
          "info": "Marque só numa opção. Se nenhuma, a primeira com produto fica selecionada."
        },
        {
          "type": "text",
          "id": "sidebar_category",
          "label": "Categoria na sidebar",
          "default": "BENCH & STAND"
        },
        {
          "type": "select",
          "id": "sidebar_icon",
          "label": "Ícone na sidebar (Material)",
          "default": "event_seat",
          "options": [
            { "value": "event_seat", "label": "event_seat (banco)" },
            { "value": "chair", "label": "chair" },
            { "value": "warehouse", "label": "warehouse (stand)" },
            { "value": "fitness_center", "label": "fitness_center" }
          ]
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Step 2: support storage",
      "category": "Product",
      "blocks": [
        {
          "type": "support_option",
          "settings": {
            "product": "adjustable-home-workout-bench",
            "card_title": "M2 Bench",
            "card_description": "Space-optimized folding design for home studios.",
            "badge_label": "",
            "default_selected": false
          }
        },
        {
          "type": "support_option",
          "settings": {
            "product": "pro-adjustable-weight-bench",
            "card_title": "M3 Pro Bench",
            "card_description": "Commercial-grade 7-position adjustability.",
            "badge_label": "",
            "default_selected": true
          }
        },
        {
          "type": "support_option",
          "settings": {
            "product": "home-workout-dumbbell-stand",
            "card_title": "Power Stand",
            "card_description": "Vertical storage for maximum space efficiency.",
            "badge_label": "",
            "default_selected": false
          }
        }
      ]
    }
  ]
}
{% endschema %}
